# دليل ربط المنظومة بقاعدة بيانات Google / Vercel وحساب Google Drive الرسمي

يوفر هذا الدليل شرحاً شاملاً لكيفية ضبط إعدادات المنظومة وربطها بقواعد البيانات السحابية وحساب Google Drive الرسمي عند رفع النظام واستضافته على سيرفرات خارجية مثل **Vercel** أو **Cloud Run**.

---

## 1. إعدادات حساب Google Drive الرسمي للتوثيق

### المميزات المتاحة من لوحة مدير النظام:
1. **حساب موحد معتمد للمنظومة**: رفع جميع شهادات التقدير تلقائياً إلى مجلد محدد على حساب Google Drive رسمي تابع للمنظومة دون إلزام المستخدمين بتسجيل الدخول بحساباتهم الشخصية.
2. **إخفاء بيانات الحساب الرسمي عن نافذة التوثيق**: خيار يسمح للمدير بإخفاء البريد الإلكتروني للحساب الرسمي واسمه من نافذة التوثيق عند المعلمين والطلاب للحفاظ على خصوصية الحساب.
3. **السماح أو منع استخدام حساب شخصي**: خيار للتحكم فيما إذا كان يُسمح للمستخدمين بالتبديل إلى حساب Google شخصي للتوثيق، أو قصر التوثيق حصراً على حساب المنظومة.
4. **فحص الاتصال الفوري**: زر مخصص في لوحة المدير لاختبار الاتصال المباشر مع واجهة Google Drive API والتحقق من صلاحية مفاتيح الوصول والوصول للمجلد.

### خطوات الربط عند النشر على Vercel:
1. ادخل إلى [Google Cloud Console](https://console.cloud.google.com/).
2. أنشئ مشروعاً وفعّل **Google Drive API**.
3. انتقل إلى **Credentials** وأنشئ **OAuth 2.0 Client ID**.
4. استخرج `Refresh Token` الدائم لحساب المنظومة الرسمي عبر [Google OAuth Playground](https://developers.google.com/oauthplayground).
5. في لوحة تحكم **Vercel** (Project Settings > Environment Variables)، أضف المتغيرات التالية:
   - `GOOGLE_DRIVE_CLIENT_ID`: معرّف العميل
   - `GOOGLE_DRIVE_CLIENT_SECRET`: السر الخاص بالعميل
   - `GOOGLE_DRIVE_REFRESH_TOKEN`: رمز التحديث الدائم
   - `GOOGLE_DRIVE_FOLDER_ID`: معرّف المجلد الهدف لتخزين الشهادات

---

## 2. ربط النظام بقواعد البيانات السحابية (Google Firestore / Vercel Postgres)

عند نشر النظام على بيئة Serverless مثل Vercel، تُعتبر قاعدة البيانات المحلية غير دائمة عبر جلسات إعادة التشغيل، ولذلك تدعم المنظومة الربط المباشر مع قواعد البيانات السحابية:

### الخيار الأول: Google Firestore (Firebase)
- **الاستخدام**: تخزين مرن، توثيق فوري للشهادات وحسابات المستخدمين مع مزامنة سحابية فائقة السرعة.
- **خطوات الربط**:
  1. توجه إلى [Firebase Console](https://console.firebase.google.com/) وأنشئ مشروعاً جديداً.
  2. فعّل **Cloud Firestore**.
  3. من إعدادات المشروع (Project Settings)، انسخ **Project ID** و **Web API Key**.
  4. في لوحة تحكم Vercel أو من لوحة مدير النظام:
     - `FIREBASE_PROJECT_ID`: معرّف المشروع
     - `FIREBASE_API_KEY`: مفتاح الواجهة البرمجية

### الخيار الثاني: Vercel Postgres / Neon
- **الاستخدام**: قاعدة بيانات علائقية متوافقة بالكامل مع بيئة Vercel بنقرة واحدة.
- **خطوات الربط**:
  1. في لوحة تحكم مشروعك على **Vercel**، انتقل إلى تبويب **Storage**.
  2. اختر **Create Database** ثم اختر **Postgres**.
  3. سيقوم Vercel تلقائياً بحقن متغير البيئة `POSTGRES_URL` في مشروعك.
  4. في لوحة مدير النظام، اختر **Vercel Postgres** واضغط على **فحص الاتصال بقاعدة البيانات** للتحقق من الاتصال وقياس سرعة الاستجابة (Latency).

---

## 3. زر فحص الاتصال واختبار الجاهزية

- يحتوي قسم **الربط السحابي وقواعد البيانات** وقسم **Google Drive** في لوحة الإدارة على أزرار فحص اتصال تفاعلية.
- عند الضغط على زر **فحص الاتصال**:
  1. يقوم الخادم بمحاولة الاتصال الحقيقي بالسيرفر السحابي أو واجهة API.
  2. يتم قياس زمن الاستجابة بالميلي ثانية (Latency).
  3. يتم عرض تقرير تشخيصي فوري يوضح حالة الاتصال، البيئة المشغلة (Vercel / Cloud Run / Node.js)، وتوصيات الصيانة إن وجدت.
